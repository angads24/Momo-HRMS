import { DashboardLayout } from '../components/DashboardLayout';

export default function AdminHomePage() {
  return (
    <DashboardLayout
      title="Admin Management"
      subtitle="Manage users, roles and system settings. This module connects to the user-management screens next."
    />
  );
}
